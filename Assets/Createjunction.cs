using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Createjunction : MonoBehaviour
{
    //Example so far creates just one junction
    string junction_id;
    string junction_fromTrackId;
    string junction_toTrackID;
    string junction_length;
    string junction_safeToSit;
    string junction_internalNodes;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
